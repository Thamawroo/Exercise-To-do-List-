import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Hapus ekstensi file .tsx
import './index.css';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
